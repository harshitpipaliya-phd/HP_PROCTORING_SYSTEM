# HP Proctoring Backend - AI Worker Requirements (HuggingFace Spaces)
# ====================================================================
# CPU-only torch — keeps HF Space image manageable.
# Install with:
#   pip install -r requirements_ai.txt
#   pip install torch==2.3.0+cpu torchvision==0.18.0+cpu --index-url https://download.pytorch.org/whl/cpu

# Web Framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Computer Vision & AI models
ultralytics>=8.2.18
mediapipe>=0.10.14
opencv-python-headless>=4.9.0.80
numpy>=1.26.4
supabase>=2.3.0

# Audio VAD — SpeechBrain for production-grade VAD (silero-based)
# Falls back to scipy signal-processing VAD when speechbrain unavailable.
speechbrain>=1.0.0
scipy>=1.13.0

# PDF Report Generation
reportlab>=4.1.0

# Cloudinary (optional — for screenshot/report upload)
cloudinary>=1.39.0

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Logging
structlog>=24.1.0

# Utilities
httpx>=0.27.0
python-dotenv>=1.0.1
