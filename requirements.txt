# HP Proctoring Backend - Requirements
# ======================================
# Python 3.10+ (spec target: 3.10, tested on 3.12)

# Web Framework
streamlit>=1.28.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
websockets>=12.0

# Pydantic (settings + validation)
pydantic>=2.6.0
pydantic-settings>=2.2.1

# Database / ORM
sqlalchemy>=2.0.30
alembic>=1.13.1
asyncpg>=0.29.0
supabase>=2.3.0

# Computer Vision & AI
opencv-python-headless>=4.9.0.80
numpy>=1.26.0
mediapipe>=0.10.9
ultralytics>=8.1.0

# Face recognition — enabled when cmake + dlib are available at build time.
# Dockerfile installs cmake; local dev: sudo apt install cmake build-essential
# If you cannot install dlib, the system gracefully falls back to MediaPipe.
face-recognition>=1.3.0
dlib>=19.24.2

# Audio Processing
soundfile>=0.12.1
speechbrain>=1.0.0  # VAD; falls back to scipy if unavailable
scipy>=1.13.0

# Machine Learning
scikit-learn>=1.4.0
joblib>=1.3.2

# Environment Variables
python-dotenv>=1.0.1

# Screen Capture
mss>=9.0.1
Pillow>=10.2.0

# HTTP (async inter-service calls)
httpx>=0.27.0

# Auth / Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# PDF Report Generation
reportlab>=4.1.0
jinja2>=3.1.4

# Media storage
cloudinary>=1.39.0

# Task Queue (async jobs)
celery[redis]>=5.3.0
redis[hiredis]>=5.0.0

# Rate limiting
slowapi>=0.1.9

# Logging & Observability
structlog>=24.1.0
sentry-sdk[fastapi]>=2.1.1

# S3 fallback (optional)
boto3>=1.34.0

# Development / Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
factory-boy>=3.3.0
ruff>=0.4.4
coverage>=7.5.0
